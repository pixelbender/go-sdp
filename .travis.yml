language: go
sudo: false
go:
  - tip
before_install:
  - go get github.com/mattn/goveralls
  - go get golang.org/x/tools/cmd/cover
  - go get github.com/golang/lint/golint
script:
  - go test -v ./...
  - $HOME/gopath/bin/goveralls -service=travis-ci -package ./sdp
  - $HOME/gopath/bin/golint ./...